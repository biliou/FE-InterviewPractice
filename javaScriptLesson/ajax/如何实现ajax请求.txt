1.创建一个XMLHttpRequest对象实例，然后通过实例的open()方法设置，请求的方式
	请求的地址，是否异步；再通过对象的send()方法来发出请求；通过监听
	readystatechange事件来获得响应；最后通过这个实例的readyState属性来判断这个
	ajax请求的状态，4为接收数据完成，这个时候可以通过实例status属性判断这个请求是否成功


2.如何实现？
	2.1	步骤
		ajax如何定义
		怎么传递参数
		怎么返回参数
		怎么解析
		如何理解json
		

3.跨域
在浏览器上当前访问的网站向另一个网站发送请求获取数据的过程就是跨域请求。
导致原因：1.域名不同；2.子域名不同；3.端口不同；4.协议不同
解决方案：1.jsonp；2.iFrame跨域；3.协议跨域 (跨域资源访问CORS)

参考资料：
https://zhuanlan.zhihu.com/p/24632196
		
		
4.ajax 是无状态，不刷新的异步请求。

无状态的意思？
有状态就是有数据存储功能。有状态对象(Stateful Bean)，就是有实例变量的对象 ，可以保存数据，是非线程安全的。在不同方法调用间不保留任何状态。
无状态就是一次操作，不能保存数据。无状态对象(Stateless Bean)，就是没有实例变量的对象 .不能保存数据，是不变类，是线程安全的。

5.什么是请求？
请求就是必须遵循某种协议（http）必须请求一个url，如果是http就会需要选择请求方式

6.请求方式
get
post
fatch
put
detele

RESTful风格